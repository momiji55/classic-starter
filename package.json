{
  "name": "classic-starter",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --config bs-config.js --host 0.0.0.0",
    "lint:css": "stylelint \"**/*.css\"",
    "lint:css:fix": "stylelint \"**/*.css\" --fix",
    "zip": "test -z \"$(git status --porcelain)\" || { echo 'コミットしていない変更があります。コミットしてから実行してください。' >&2; exit 1; }; mkdir -p dist && git archive --format=zip --prefix=classic-starter/ -o dist/classic-starter.zip HEAD && echo 'dist/classic-starter.zip を作成しました。'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "stylelint": "^17.6.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-order": "^8.1.1"
  }
}
